# Copy this file to  ghl.env  (in the repo root) and fill in your values.
#   cp samples/springs-creative/ghl.env.example ghl.env
# ghl.env is gitignored, so it stays on your Mac and is never pushed.
# autopost.sh reads these automatically so you don't re-paste them each time.

GHL_TOKEN=pit-your-token-here
GHL_LOCATION_ID=ibvPpyoxFegC4gujJX5f
